import React from "react";

const Home = () => {
  const handleLogout = () => {};
  return (
    <div>
      <h1>You are logged in.</h1>
      <button onClick={handleLogout}></button>
    </div>
  );
};

export default Home;
